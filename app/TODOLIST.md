# TODOLIET

## 1. 创建线程类，来管理所有的线程创建，包含java和native层(java包含native)
## 2. 录制，编解码
## 3. zoom放大矩阵(拍摄、录制的时候都要支持)，对焦，闪光灯功能
## 4. 区分renderSize、previewSize、captureSize
## 5. 需要一个资源管理类
## 6. 现阶段的Camera2Proxy有点庞大，需要将其进行解耦，设计一个可以直接使用的camera的抽象类